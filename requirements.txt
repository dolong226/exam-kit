# --- Core & Data Validation ---
pydantic>=2.0.0  # Định nghĩa và validate dữ liệu (models.py) rất mạnh mẽ
PyYAML>=6.0      # (Tùy chọn) Rất khuyên dùng YAML cho test bank vì dễ viết, dễ đọc hơn JSON

# --- CLI & Terminal UI ---
typer>=0.9.0     # Tạo CLI command cực nhanh và dễ (dựa trên type hints)
rich>=13.0.0     # Render terminal đẹp (màu sắc, bảng biểu, in đậm) cho cli/display.py

# --- AI / LLM (Dành cho phần Tự luận sau này) ---
# google-genai>=0.3.0  # Tích hợp Gemini để chấm điểm
# openai>=1.0.0